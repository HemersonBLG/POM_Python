Automation made in Python 3.10

System Requires:
    -Python 3.10
    -Selenium
    -Chromedriver_autoinstaller
    
After install Python 3.10 install selenium and Chromedriver_autoinstaller with the comands:
pip install selenium
pip install chromedriver-autoinstaller

To run the Automation, open the directory of the file in the terminal and run the comand: python main.py